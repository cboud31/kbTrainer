import React from 'react';
import { NavBar, Header } from '../../components/';

const Home = () => {
  return (
    <div>
      <NavBar />
      <Header />
    </div>
  );
};

export default Home;

// This is the Home page. It is the first page users see when they visit the site.
// The Home page will be made of 3 main components: Menu, Header, and Footer.
